# 환경변수 예시 파일 (Git에 포함)
#
# 사용 방법:
# 1. 이 파일을 복사: cp .env.example .env.development
# 2. 로컬 개발: .env.development 생성 (Git 제외됨)
# 3. 프로덕션: .env.production 생성 (Git 제외됨)

# 백엔드 API URL
# 로컬: http://localhost:3000
# 프로덕션: http://your-ec2-ip:3000 또는 https://api.yourdomain.com
VITE_API_URL=http://localhost:3000
